# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example
# 生产环境变量**模板**（只有占位符，没有真密钥）—— 填好的 .env 仍然被忽略
!.env.production.example

# 本地私密文件：含服务器公网 IP、端口扫描结果、ICP 备案状态、Azure 租户 ID
# 以及 AppID 审核策略。**仓库以后要转公开**，所以这些不能进版本库。
# 文件保留在本地（部署和填表时还要用）。
appid表单.txt

# 本地环境覆盖：docker-compose.override.yml 常被用来写死本机端口/路径
docker-compose.override.yml

# 日志与数据库转储（排查问题时会在根目录生成，不该进版本库）
*.log
*.sql.gz
*.dump
# 数据库备份目录（`debug.bat backup` 的默认输出位置）
/backups/

# 编辑器 / 助手工具的本地配置：每个开发者机器上都不一样，
# 提交进来只会给审阅的人增加噪音
.idea/
.vscode/
.claude/
.cursor/

# 系统杂物
Thumbs.db
Desktop.ini

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
