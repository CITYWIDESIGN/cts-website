# CTS 服务器官网 · 项目介绍

## 一句话

**CTServer 是一个 Minecraft 生电服务器的官方网站**——玩家在这里了解服务器、
申请加入、审核通过后分享和下载红石机器的投影文件。

---

## 二、这个网站解决什么问题

CTS 是一个**私有邀请制**的 Minecraft 服务器，主打**生电**（红石与自动化）和**建筑**。
Fabric 服务端、原版机制、**没有经济系统、没有插件**。

这类服务器有个共同的门槛：**不能谁都能进**。玩生电需要长期在线、协作建大型机器，
一个乱来的人能毁掉几个月的工程。所以传统做法是靠 QQ 群人工拉人——但那样：

- 问一遍基础问题要重复几十次，回答还散落在聊天记录里
- 没法留下"这个人当时是怎么答的"的记录
- 玩家问"服务器什么配置、有什么规则"要反复解释

**这个网站把这些流程化了**：一份固定的审核问卷 → 后台审核 → 通过后自动获得
社区权限。同时它也是服务器的门面——展示配置、玩法、建筑作品。

> 附带一个现实原因：申请 Minecraft 的 AppID（用于"用 Microsoft 账号登录"）需要一个
> 可访问的官方网站，这个站就是为此而建。

---

## 三、用户角色

| 角色 | 能做什么 |
|---|---|
| **访客**（未登录） | 看首页、服务器介绍、规则、资源列表、社区信息、玩家公开资料页 |
| **注册用户** | 提交入服申请、上传资源、点赞评论、改自己的资料 |
| **服务器成员** | 审核通过后的玩家，可下载资源、参与社区 |
| **管理员** | 审核申请、管理用户/资源/举报、编辑全站内容、看审计日志 |

---

## 四、完整旅程

```
访客
 └─ 首页了解服务器 → 看服务器配置和规则
     └─ 注册（本地账号 或 Microsoft 登录）
         └─ 填写「CTS 服务器成员审核」问卷（16 题）
             └─ 管理员在后台审核 → 通过 / 拒绝
                 └─ 通过后：下载他人的投影、上传自己的作品
```

---

## 五、功能详解

### 1. 首页

网站的"第一印象"，内容几乎全部由管理员在后台配置：

- **轮播图**——管理员上传图片 + 文案，可排序、可上下架
- **服务器状态卡片**——显示**真实在线人数**（不是写死的数字）
- **社区数据**——累计玩家、建筑作品、社区成员、稳定运行天数（管理员填）
- **特色介绍**——这个服务器和别处有什么不同
- **加入流程**——把"怎么进服"讲清楚
- **最新公告**——管理员可发布，支持草稿态
- **常见问题**

### 2. 服务器 / 规则

- **服务器页**：配置（版本、端口、正版验证）、玩法介绍、游戏模式
- **规则页**：入服规则、处罚条款

这两块的内容都是**中英双语**、管理员可改。中文必填、英文可选，
没填英文就自动回退中文——管理员可以用中文先写起来，不用等翻译。

### 3. 资源分享（核心功能）

玩家在这里分享红石机器的投影文件、建筑存档、整合包等。

**浏览侧**
- 列表 + 搜索（标题/介绍/文件名/上传者）+ 分页
- 卡片缩略图、下载量、点赞数、评论数
- 详情页：介绍、封面图、投影预览、下载按钮、点赞、评论区、举报

**上传侧**
- 上传附件 + 可选封面图
- **每日发布次数**和**每日上传流量**双重配额（管理员不受限）
- 编辑（可替换附件，保留版本修改历史）、删除

**互动侧**
- 点赞
- 评论（支持一层回复，不做无限楼中楼）
- 评论点赞
- 举报（管理员在后台处理）

**防滥用**
- 每日次数 + 每日流量配额，按「天」分行记录，天然实现每天重置
- 下载同样计入流量配额
- 上传接口在解析请求体**之前**先看 `Content-Length`，防止超大请求打爆内存

### 4. 投影预览（最有特色的功能）

**这是这个站最花心思的地方。**

红石玩家分享的 `.litematic` 是 Litematica 模组的投影文件——本质是一堆方块坐标
和方块类型。**光看文件名和介绍，根本不知道里面是什么机器。**

所以这个站会**自动生成预览图**：

- 上传 `.litematic` 后，在**资源详情页展开预览**时自动渲染
- 生成 **4 个方向**的等轴测图，一眼能看清机器的立体形状
- 用**真实的方块材质和模型**——楼梯是楼梯、台阶是台阶、栅栏是栅栏，
  不是"一切皆方块"的粗糙示意
- **背景透明**，直接融进页面底色，跟随明暗主题
- 图片**可以点开铺满整个屏幕**看细节
- 同时提取出**投影元信息**：名称、作者、尺寸（如 304×126×304）、方块总数、区域数

**几个不显然的设计决定：**

- **默认折叠**。预览不是必需品，而它的代价不小（要下载模型 + 3MB 材质包 + 渲染）。
  折叠状态下不想看的人**一点代价都不付**——连模型都不会下载。
- **渲染在浏览器里做，不在服务器上**。渲染需要 WebGL，服务器上要跑就得装
  headless WebGL，在部署环境里非常折腾还可能搞坏现有服务。而用户本来就在浏览器里，
  顺手渲染掉。
- **渲完存回服务器**。第一个打开的人渲一次，后面所有人秒开——代价只落在第一个
  感兴趣的人身上，而不是每个上传者身上。
- **元数据由服务器自己从文件里解**，不采信浏览器提交的数值。

### 5. 社区

汇总 Discord / OOPZ / QQ 群的加入方式。QQ 群二维码是**矢量生成**的，
跟着主题配色走，不是一张贴图。

### 6. 账号体系

**两种登录方式并存：**

- **本地账号**——用户名 + 邮箱 + 密码。邮箱验证码验证。
  - 用户名规则：**3–20 位，允许中文、字母、数字、下划线、连字符**，
    **大小写敏感**（`Alice` 和 `alice` 是两个账号）
  - 密码用 `scrypt` 哈希（N=65536），不可逆
- **Microsoft 登录**——验证 Minecraft 正版身份，**绑定后解锁头像框**

**账号操作：**
- 改用户名（每天 1 次）
- 换绑邮箱（新旧邮箱**双验证码**）
- 注销账号（可保留内容并匿名化）
- 被封禁时有明确提示（限期 / 永久，到期自动失效）

**安全设计：**
- 会话是**无状态 cookie**，靠 `sessionVersion` 判断是否被作废——
  改密码后旧 cookie 立刻失效，不需要黑名单
- 登录失败有**陪跑逻辑**，让"账号不存在"和"密码错误"耗时接近，
  防止通过响应时间探测账号是否存在
- 登录、发验证码都有**按 IP 和按账号的双重限流**

### 7. 入服审核问卷

这是网站的核心流程。

**当前问卷**：「CTS 服务器成员审核」，**16 题 30 选项**：

| 题 | 类型 |
|---|---|
| 1. Minecraft Java ID | 填空 |
| 2. 熟练掌握的辅助模组、工具 | 多选（11 项：Tweakeroo / Litematica / Carpet…） |
| 3. 是否常驻其他生存服务器 | 单选 |
| 4. 能提供哪些方面的帮助 | 多选（7 项，含详细的引导说明） |
| 5. 平均每天游玩时长 | 单选（6 档） |
| 6. 通常的上线时间段 | 多选（4 个时段） |
| 7. 如何检查机器中没有错误方块（Litematica 场景） | 长文本 |
| 8. 损坏了重要机器会如何处理 | 长文本 |
| 9. 上线发现只有自己在线会做什么 | 长文本 |
| 10. 和成员产生分歧会如何处理 | 长文本 |
| 11. 如何评价自己的「肝度」 | 填空 |
| 12. 年龄 | 填空 |
| 13. QQ 昵称 | 填空 |
| 14. QQ 号码 | 填空 |
| 15. 请简单介绍一下自己 | 长文本 |
| 16. 我有特殊的入服理由（选填） | 长文本 |

**设计取向：以主观题为主。** 客观题（模组、时长、时段）只用来了解背景，
真正判断的是第 7–10、14 题——**这些问题没有标准答案，但答得好不好一眼能看出来**，
考察的是：对生电的理解、责任心、主动性、协作能力。

**流程：**
- 管理员在后台建问卷、加题目和选项、控制发布状态
- 用户填写 → 提交 → 后台看到待审列表
- 管理员逐份审核 → 通过 / 拒绝（可写备注）
- 一人一份（数据库唯一约束保证）

### 8. 站内消息

右上角铃铛，收到：
- 举报处理结果
- 你的资源被点赞
- 你的评论被回复

已读的通知会被定期清理。

### 9. 管理后台

管理员的一站式控制台：

| 模块 | 能做什么 |
|---|---|
| **仪表盘** | 待处理的申请、举报、最新动态 |
| **用户管理** | 改角色、封禁/解封、按**账号名 / 玩家名 / UUID** 检索 |
| **资源管理** | 查看、编辑、删除任意资源 |
| **问卷管理** | 建问卷、编题目选项、发布状态、看提交 |
| **提交审核** | 逐份审阅申请，通过 / 拒绝 |
| **举报处理** | 处理用户举报 |
| **审计日志** | 谁在什么时候删了什么（保留 365 天） |
| **内容管理** | 首页数字、服务器介绍与配置、规则（中英双语） |
| **公告 / 轮播图** | 增删改、排序、草稿态 |
| **加入入口 / 限额** | 配置入服引导和各项数值限制 |
| **统计** | 活跃度数据 |

**「限额」是全站可配的**，不写死在代码里：
每日上传次数、每日流量、附件大小上限、封面图大小上限……管理员改一个数字，
前面所有相关提示文字（包括"最多 N MB"这类文案）都会跟着变。

---

## 六、设计规范

**文案分两档语气，不混用：**

- **界面 / 功能**（导航、标题、按钮、状态、报错、后台）→ 商务、克制、说明性。
  例：「暂无资源」
- **提示语**（输入框 placeholder 和 hint）→ 口语化，且**一律不举例子**。
  例：「给这个资源起个名字」

**数字不写死在文案里**。曾经把「1MB」「1GB」写死在提示里，限额变成可配置之后
这些文案就开始说谎了。现在一律用参数。

**全站中英双语**，无 URL 前缀，靠 cookie 记住语言选择。

---

## 七、技术架构（简版）

| | |
|---|---|
| 前端 / 后端 | Next.js 16（App Router）+ React 19 + TypeScript |
| 样式 / 动画 | Tailwind v4 + shadcn/ui + Motion |
| 数据库 | PostgreSQL 16 + Prisma |
| 会话 | 无状态加密 cookie（iron-session） |
| 邮件 | QQ 邮箱 SMTP（验证码） |
| 部署 | Docker Compose + Cloudflare Tunnel |

**几个值得一提的技术选择：**

- **零端口发布**。服务器上一个端口都不对外开，网站通过 Cloudflare 隧道
  **主动连出去**。好处是攻击面极小，也不需要配证书。
- **服务器状态是真探测的**。自己实现了 Minecraft 的 Server List Ping 协议，
  直接问服务器要真实在线人数，不依赖第三方 API、不需要装插件。
- **二进制全进数据库**（附件、封面、预览图）。好处是不依赖共享文件系统，
  部署只有一个容器。
- **投影渲染用 lodestone**（一个 TypeScript 的 Minecraft 结构渲染库，
  基于 deepslate），不自己解析 NBT 格式。

---

## 八、当前状态

**已上线**：<https://web.ctserver.top>

- 全站 HTTPS（HTTP 自动跳转）
- 完整的安全响应头（HSTS、CSP、nosniff 等）
- 数据库、网站、隧道三个容器常驻运行

**已知限制：**

- **Microsoft 登录的最后一步会 403**——Minecraft 的 AppID 还在审批中。
  本地账号完全不受影响，可以先注册使用。
- **站点在国内访问偏慢**（约 1 秒起）——Cloudflare 把这条线路的流量甩到了美西节点，
  每个请求要跨太平洋两趟。已做边缘缓存优化，但根治需要换网络路径。

**规划中：**

- AppID 审批通过后开放 Microsoft 登录
- 域名备案（影响能否用国内节点加速）
- 备份异盘存放 + 定时任务

---

## 九、一句话总结每个模块

| 模块 | 一句话 |
|---|---|
| 首页 | 服务器门面，内容全部后台可配 |
| 服务器 / 规则 | 静态内容，中英双语 |
| 资源 | 投影和存档的分享平台，带配额和互动 |
| **投影预览** | **自动把 .litematic 渲染成 4 方向立体图，透明底、可点开** |
| 社区 | Discord / OOPZ / QQ 群入口 |
| 账号 | 本地账号 + Microsoft 双通道，可绑定正版身份 |
| **审核问卷** | **16 题主观题为主的入服申请，后台逐份审** |
| 站内消息 | 举报结果、被点赞、被回复 |
| 管理后台 | 用户 / 资源 / 问卷 / 举报 / 审计 / 全站内容 |
